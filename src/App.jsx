/**
 * Placeholder shell. Subtask C replaces this with the real React port of
 * `src/app.js` (which is intentionally left in the tree, un-imported, until
 * then).
 */
export default function App() {
  return (
    <main className="mx-auto max-w-[820px] px-4 py-8">
      <h1 className="text-2xl font-semibold">Logos Seeker</h1>
      <p className="mt-2 text-[var(--color-muted)]">React port pending.</p>
    </main>
  );
}
